local colors = require("colors")

sbar.bar {
  height = 40,
  position = "bottom",
  padding_right = 10,
  padding_left = 10,
  color = colors.transparent,
  border_color = colors.transparent,
  margin = 10,
  corner_radius = 12,
  topmost = "on",
}