#!/usr/bin/env bash
# Return url of selected browser tab

# @vicinae.title Put URL
# @vicinae.mode compact
# @vicinae.schemaVersion 1

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$script_dir/browser" url
