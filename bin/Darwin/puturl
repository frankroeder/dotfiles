#!/usr/bin/env bash
# Return url of selected browser tab

# @raycast.title Put URL
# @raycast.mode compact
# @raycast.schemaVersion 1

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$script_dir/browser" url
