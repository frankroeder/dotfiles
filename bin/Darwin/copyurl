#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
url="$("$script_dir/browser" url)"

printf '%s' "$url" | pbcopy
printf 'Copied %s to clipboard\n' "$url"
