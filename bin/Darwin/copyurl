#!/usr/bin/env osascript
# Copy url of selected tab in brave

tell application "Brave Browser"
  set vURL to URL of active tab of front window
  set the clipboard to vURL
  "Copied " & vURL & " to clipboard"
end tell
