# Identity profile: no tint until Super+Ctrl+N (asahi-nightlight).
# Off = identity. On = temperature below (toggle reads it even if commented).
profile {
    time = 07:00
    identity = true
}

# Clock-based: uncomment (hyprsunset already starts from autostart.lua).
# profile {
#     time = 20:00
#     temperature = 1500
# }
