# fnmode=1 (fkeyslast): volume/brightness/media on the top row, F1–F12 behind Fn.
# Matches macOS. Hyprland binds XF86MonBrightness* / XF86Audio*; they do nothing
# if the top row emits F-keys (fnmode=2 / fkeysfirst).
options hid_apple fnmode=1
