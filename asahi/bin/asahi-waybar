#!/usr/bin/env bash

set -euo pipefail

cache="${XDG_CACHE_HOME:-$HOME/.cache}/asahi-theme"

"$HOME/.dotfiles/asahi/bin/asahi-theme" auto --no-restart
exec waybar -c "$HOME/.config/waybar/config.jsonc" -s "$cache/waybar/style.css"
