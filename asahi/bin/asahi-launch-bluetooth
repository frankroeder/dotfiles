#!/usr/bin/env bash

set -euo pipefail

if command -v blueman-manager >/dev/null 2>&1; then
  exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/asahi-launch" blueman-manager
fi

notify-send "Bluetooth" "blueman-manager is not installed."
exit 1
